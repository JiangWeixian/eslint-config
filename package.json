{
  "name": "@aiou/eslint-config-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "description": "monorepo for aiou templates",
  "scripts": {
    "lint": "eslint -c eslint.config.mjs",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ignore": "eslint . --config=./packages/default/index.js",
    "test:react": "eslint fixtures --config=./packages/react/index.js",
    "build": "pnpm --filter=./packages/ignore --filter=./packages/default build",
    "ci:version": "pnpm changeset version",
    "ci:publish": "pnpm run build && pnpm changeset publish",
    "ci:snapshot": "pnpm changeset version --snapshot snapshot",
    "ci:prerelease": "pnpm run build && pnpm changeset publish --no-git-tag --snapshot",
    "prepare": "husky install"
  },
  "lint-staged": {
    "**/**/*.{js,ts,tsx,vue,json,md}": ["eslint --fix"]
  },
  "dependencies": {
    "esbuild": "^0.17.19",
    "typescript": "^5.9.2"
  },
  "devDependencies": {
    "@aiou/eslint-config": "workspace:*",
    "@aiou/eslint-plugin-progress": "workspace:*",
    "@changesets/cli": "^2.26.0",
    "cz-emoji": "^1.3.1",
    "eslint": "^10.2.1",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "vitest": "^3.2.1"
  },
  "pnpm": {
    "overrides": {
      "eslint": "10.2.1"
    }
  },
  "config": {
    "commitizen": {
      "path": "node_modules/cz-emoji"
    }
  }
}
